@page "/"

<div class="landing-wrapper">
    <h1 class="landing-title">Welcome to Furniture Store</h1>
    <p class="landing-subtitle">Discover elegant designs for modern living</p>
    <div class="landing-actions">
        <a class="landing-btn primary" href="/home">Enter Store</a>
        <a class="landing-btn accent" href="/login">Login</a>
        <a class="landing-btn primary" href="/create-account">Register</a>
    </div>
</div>

<style>
    .landing-wrapper {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 50px 16px;
        background: #f5f5f5;
    }

    .landing-title {
        color: #603F26;
        font-size: clamp(2.5rem, 5vw, 3rem);
        margin-bottom: 20px;
    }

    .landing-subtitle {
        font-size: clamp(1.1rem, 2.5vw, 1.25rem);
        color: #666;
        margin-bottom: 30px;
    }

    .landing-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 16px;
        justify-content: center;
        margin-top: 40px;
    }

    .landing-btn {
        display: inline-block;
        padding: 15px 30px;
        border-radius: 5px;
        text-decoration: none;
        font-weight: 600;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .landing-btn.primary {
        background: #603F26;
        color: #fff;
    }

    .landing-btn.accent {
        background: #FFB4A2;
        color: #603F26;
    }

    .landing-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(0, 0, 0, 0.15);
    }
</style>
